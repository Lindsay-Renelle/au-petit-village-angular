import { SortBySearchPipe } from './sort-by-search-pipe';

describe('SortBySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
